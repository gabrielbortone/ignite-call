import { styled } from "@stitches/react";

export const CalendarItemDayItem = styled('span', {
    fontSize: '1.1rem',
    fontFamily: 'Roboto',
    alignItems: 'center',
    justifyContent: 'center',
    display: 'flex',
});